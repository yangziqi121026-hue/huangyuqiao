# ===== A股只读投研分析 Agent 环境变量 =====
# 复制为 .env 后填写。只需要 LLM 相关，不需要任何券商 / 交易 Key。

# LLM（OpenAI 兼容协议）。留空则自动进入 mock 模式，仍可跑通全流程。
OPENAI_API_KEY=
# 可换成 DeepSeek / Qwen / 智谱 等兼容端点：
#   DeepSeek: https://api.deepseek.com/v1
#   Qwen:     https://dashscope.aliyuncs.com/compatible-mode/v1
OPENAI_BASE_URL=https://api.openai.com/v1
MODEL_NAME=gpt-4o-mini

# 强制 mock（即使配了 key 也走 mock，便于离线测试）
MOCK_MODE=false

# ===== 安全说明 =====
# 本项目不读取、不需要、也不允许配置任何：
#   - 券商 API Key（IBKR / 富途 / 老虎 等）
#   - 交易所 Key（OKX / Binance 等）
#   - live / 实时行情订阅凭证
# 如果你在这里看到上述任何 Key，请删除——本系统只做只读分析。
